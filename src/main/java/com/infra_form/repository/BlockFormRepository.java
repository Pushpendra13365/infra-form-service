package com.infra_form.repository;

import com.infra_form.model.BlockForm;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BlockFormRepository extends JpaRepository<BlockForm, Long> {
}
